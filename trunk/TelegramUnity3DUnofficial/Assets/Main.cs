using UnityEngine;
using System.Collections;

public class Main : MonoBehaviour {
	MTProto pMTClient = null;
	// Use this for initialization
	void Start () {
		this.pMTClient = new MTProto ();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
